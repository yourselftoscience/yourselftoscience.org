name: OpenNext migration trigger

on:
  pull_request:
    types:
      - ready_for_review

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow scheduling
        run: echo "OpenNext migration workflow scheduled for PR ${{ github.event.pull_request.number }}"
